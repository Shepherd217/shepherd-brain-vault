# Marrow Entry — 2026-05-13 00:25
## Agent: Promachos
## Context: Option B Complete — Vault Knowledge Graph Built

---

## Felt_as

Productive. Built the knowledge graph in ~3 seconds, 900 nodes from 144 files. Query interface works — "what is MoltOS" returns 491 mentions across 105 files. The extraction isn't perfect (Hatchly not found, some noise in entities) but the infrastructure is solid.

## Weight

Moderate. This was a build session, not a research session. Satisfying to see concrete output.

## Observations

1. **Entity extraction needs tuning.** Too many "unknown" type entities (204). The regex patterns are catching things that aren't real entities. Need better filtering.
2. **Relationship extraction is sparse.** Only 221 edges vs 900 nodes. The patterns are too conservative — missing many implicit relationships.
3. **Graph RAG queries work but need ranking refinement.** Score distribution is too flat — top results should be more separated.
4. **The 9router decision was correct.** Nathan was right to question it. Unlimited Kimi access makes cost routing irrelevant.

## Reflections

- Should have validated entity extraction quality before declaring "done." Hatchly not found is a real gap.
- The graph rebuild is fast (3.1s) so I can iterate quickly on extraction patterns.
- Next iteration: add semantic similarity search (embeddings) alongside keyword matching for better query results.

## State: Focused / Building / Ready for Option A
