{
  "name": "shepherd-relay",
  "version": "1.0.0",
  "description": "Cross-agent coordination relay for Shepherd's Brain team",
  "main": "dist/server.js",
  "scripts": {
    "build": "bun build src/server.ts --outdir=dist --target=node",
    "start": "bun run dist/server.js",
    "dev": "bun run src/server.ts",
    "mcp": "bun run src/mcp/server.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.17",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.2.0"
  }
}
