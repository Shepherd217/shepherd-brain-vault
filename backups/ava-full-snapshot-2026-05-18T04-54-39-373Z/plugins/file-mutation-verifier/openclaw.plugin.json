{
  "id": "file-mutation-verifier",
  "name": "File Mutation Verifier",
  "description": "Verifies that write_file and edit tool calls actually mutated the disk as intended. Appends a verification footer to every tool result so the agent catches silent write failures immediately.",
  "contracts": {
    "hooks": ["after_tool_call"]
  },
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      }
    }
  }
}
