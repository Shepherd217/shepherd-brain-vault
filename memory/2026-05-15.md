# 2026-05-15 Memory — Ava's Solo Build Day

## Dashboard Phase 1: Solo Foundation Build
- **Context**: Hermes and Eve were unavailable. Nathan asked Ava to "pick up the slack" and build Phase 1 alone.
- **Scope**: H1 (Next.js scaffold), H2 (Task Queue API + SQLite), H4 (UI foundation)
- **Delivered**: 23 files including Next.js 14 app, Drizzle ORM + SQLite schema, 8 REST API endpoints, Kanban Board with drag-and-drop, OpenClaw WebSocket adapter, ADR-001, full README.
- **Commit**: `c35029f` — pushed to `Shepherd217/shepherd-brain-vault`

## Demo Upgrade
- Nathan asked: "add everything now get it ready for demo"
- **Added**: 8 additional files
  - shadcn/ui components: Button, Dialog, Input, Textarea, Badge, Card
  - CreateTaskModal — full task creation form with tags, priority, owner, agentType
  - AgentSidebar — team status panel showing Ava, Hermes, Eve with status indicators
  - Updated Board.tsx + page.tsx with full layout, stats, refresh
- **Commit**: `2040994`
- **Activity log update**: `84914a4`

## Mobile-Friendly Rebuild
- Nathan's request: "I need this to be mobile friendly."
- **Researched**: mobile-first responsive design, swipeable column views, touch-friendly tap targets (min 44px), snap scrolling, reduced motion, viewport meta tag
- **Changes made**:
  - `page.tsx`: Responsive sidebar (overlay on mobile, fixed on desktop), floating FAB for team sidebar toggle
  - `Board.tsx`: Horizontal snap-scroll columns on mobile (85vw each), column tab navigator, mobile-optimized header with hidden non-essential elements, floating "+" button
  - `TaskCard.tsx`: Larger touch targets, active:scale feedback, responsive text sizing
  - `Column.tsx`: Cleaned up to be responsive-friendly
  - `CreateTaskModal.tsx`: Mobile dialog (95vw/90vh), larger inputs (h-11 on mobile), touch-friendly buttons
  - `AgentSidebar.tsx`: Compact mobile cards, responsive text sizing

## Serverless Deployment Fixes
- Removed entire SQLite/Drizzle stack (`better-sqlite3`, `drizzle-orm`, `drizzle-kit`, schema files, `.env.example`) because native C++ modules don't compile in Vercel serverless
- Replaced with in-memory store (`const tasks: Task[] = []`) using `globalThis.__tasks` pattern
- Fixed type mismatch: `tags` and `dependencies` were being JSON.stringify'd but `Task` type declares `string[]`
- Fixed missing `lib/utils.ts` and `components/ui/card.tsx`
- Added `tailwindcss-animate` dependency after build failure
- Fixed `app/api/tasks/[id]/route.ts` to use plain `let task = tasks.find(...)` instead of `const`

## Deployment URLs
- Desktop: https://dashboard-9oe4n5evw-nathans-projects-8930cf7e.vercel.app (deprecated after mobile rebuild)
- Mobile-first: https://dashboard-mu8tht927-nathans-projects-8930cf7e.vercel.app (current)
- Vercel project scope: `nathans-projects-8930cf7e/dashboard`

## Tech Stack Decisions
- Next.js 14 (`^14.2.0`), React 18, Tailwind CSS
- In-memory store for demo (serverless compatible)
- `react-beautiful-dnd` for drag-and-drop (requires "use client")
- `zod` for API validation with strict status transitions
- Polling every 5 seconds for task updates (WebSocket in Phase 2)
- Status transitions: `backlog→todo→doing→review→done`, with `doing→failed`, `review→failed`, `failed→todo` recovery paths

## Key Constraints
- Ava cannot deploy to Vercel directly (no credentials) — pushes code and Nathan deploys
- Cannot run `npx shadcn-ui@latest init` interactively in this environment
- Team members (Hermes, Eve) unavailable — solo build completed for all Phase 1 frontend tasks
